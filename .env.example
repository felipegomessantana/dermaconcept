# Server-side only (Vercel /api). Do NOT use the VITE_ prefix — it exposes the key in the browser.
RESEND_API_KEY=re_xxxxxxxx

# Sender (verify your domain in Resend for production)
# Without a verified domain, use: Derma Concept Academy <onboarding@resend.dev>
RESEND_FROM_EMAIL=Derma Concept Academy <contato@dermaconcept.com.br>

# Inbox that receives form submissions
CONTACT_TO_EMAIL=contato@dermaconcept.com.br
